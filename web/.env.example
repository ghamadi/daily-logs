# Supabase — from the Supabase project's API settings
NEXT_PUBLIC_SUPABASE_URL=https://project-id.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=supabase-publishable-key

# Dev database — docker-compose.yml
DATABASE_URL=postgresql://postgres:postgres@localhost:5442/main
# Test database — docker-compose.test.yml -- `getDatabaseUrl()` selects TEST_DATABASE_URL when NODE_ENV=test
TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5443/test_db

# Vercel AI Gateway
AI_GATEWAY_API_KEY=ai-gateway-api-key

# E2E auth bypass — DO NOT set this in any real environment.
# It is turned on automatically by playwright.config.ts's webServer (value '1')
# for the localhost E2E dev server only, and is inert unless NODE_ENV != production.
# When on, the app trusts the `dl-e2e-session` test cookie instead of a Supabase JWT.
# E2E_AUTH_BYPASS=1
